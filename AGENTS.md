# AGENTS.md — AI Context for MagiMontessori

This file helps AI coding tools (Claude Code, Cursor, Copilot) understand
this project's architecture. Read this before making changes.

## Project overview

**MagiMontessori** — Personalized Montessori learning adventures for every child

MagiMontessori creates AI-powered interactive learning books tailored to each child's age, interests, and developmental stage. Parents and educators get a magical toolkit that makes Montessori principles come alive at home or in the classroom.

Generated by Skylia AI Startup Builder. Full-stack Next.js SaaS application.

## Tech stack

  - ai: OpenAI
  - auth: Clerk
  - email: Resend
  - hosting: Vercel
  - database: Neon
  - payments: Stripe
  - analytics: PostHog
  - monitoring: Sentry
  - Framework: Next.js 14 (App Router) + TypeScript
  - Styling: Tailwind CSS + shadcn/ui
  - Deployment: Vercel

## Directory structure

```
src/
  app/              # Next.js App Router pages and API routes
    api/            # API route handlers
    dashboard/      # Authenticated dashboard pages
  components/
    ui/             # shadcn/ui components (Button, Input, Card, etc.)
  lib/              # Shared utilities (db client, stripe, email, etc.)
prisma/             # Database schema and migrations
public/             # Static assets
```

## Key files

- `src/app/layout.tsx` — Root layout with providers (auth, theme)
- `src/app/page.tsx` — Landing page
- `src/app/dashboard/page.tsx` — Main authenticated dashboard
- \`middleware.ts\` — Clerk auth middleware (protects /dashboard routes)
- \`src/app/api/webhooks/stripe/route.ts\` — Stripe webhook handler
- \`prisma/schema.prisma\` — Database schema
- \`src/lib/db.ts\` — Prisma client singleton
- `.env.example` — Required environment variables

## Database models

  - `User`: id, clerkId, email, name, avatarUrl, stripeCustomerId, createdAt, updatedAt
  - `Workspace`: id, name, slug, plan, logoUrl, createdAt, updatedAt
  - `WorkspaceMember`: id, workspaceId, userId, role, joinedAt, createdAt, updatedAt
  - `Subscription`: id, workspaceId, stripeSubscriptionId, stripePriceId, status, seatCount, currentPeriodEnd, createdAt, updatedAt
  - `ChildProfile`: id, workspaceId, name, dateOfBirth, avatarUrl, interests, montessoriLevel, totalBooksRead, createdAt, updatedAt
  - `LearningBook`: id, workspaceId, childProfileId, title, topic, montessoriArea, ageGroup, content, coverImageUrl, status, completedAt, createdAt, updatedAt
  - `ActivityCard`: id, bookId, title, instructions, materials, montessoriArea, difficulty, printableUrl, createdAt, updatedAt

## Pages

  - `/` — Landing
  - `/onboarding` — Onboarding (auth required)
  - `/dashboard` — Dashboard (auth required)
  - `/dashboard/children` — Child Profiles (auth required)
  - `/dashboard/children/[id]` — Child Detail (auth required)
  - `/dashboard/books` — Books Library (auth required)
  - `/dashboard/books/[id]` — Book Reader (auth required)
  - `/dashboard/books/new` — Create Book (auth required)
  - `/dashboard/activities` — Activities (auth required)
  - `/dashboard/reports` — Progress Reports (auth required)
  - `/settings` — Settings (auth required)
  - `/billing` — Billing (auth required)

## Conventions

- Use `cn()` from `src/lib/utils.ts` for conditional classNames
- All UI components are in `src/components/ui/` (shadcn/ui pattern)
- API routes use Next.js Route Handlers (`route.ts` files)
- Protected pages check auth in server components or middleware
- Environment variables are typed in `.env.example`

## Common tasks

- **Add a new page**: Create `src/app/<path>/page.tsx`
- **Add an API route**: Create `src/app/api/<path>/route.ts`
- **Add a UI component**: Use shadcn/ui CLI or create in `src/components/ui/`
- **Modify database**: Edit `prisma/schema.prisma`, then run `npm run db:push`
- **Add env variable**: Add to both `.env.example` and `.env.local`
