# MagiMontessori

> Personalized Montessori learning adventures for every child

MagiMontessori creates AI-powered interactive learning books tailored to each child's age, interests, and developmental stage. Parents and educators get a magical toolkit that makes Montessori principles come alive at home or in the classroom.

Generated by **[Skylia](https://skylia.dev)** AI Startup Builder.

## Stack

| Layer | Service |
|-------|---------|
| Framework | Next.js 14 + TypeScript |
| Styling | Tailwind CSS + shadcn/ui |
| Hosting | Vercel |
| Database | Neon + Prisma |
| Auth | Clerk |
| Payments | Stripe |
| Email | Resend |
| AI | OpenAI |


## Getting started

```bash
npm install
cp .env.example .env.local
# Fill in your environment variables
npm run dev
```

## Environment variables

```
DATABASE_URL=
NEXT_PUBLIC_CLERK_PUBLISHABLE_KEY=
CLERK_SECRET_KEY=
CLERK_WEBHOOK_SECRET=
STRIPE_SECRET_KEY=
NEXT_PUBLIC_STRIPE_PUBLISHABLE_KEY=
STRIPE_WEBHOOK_SECRET=
STRIPE_FAMILY_PRICE_ID=
STRIPE_CLASSROOM_PRICE_ID=
OPENAI_API_KEY=
RESEND_API_KEY=
NEXT_PUBLIC_POSTHOG_KEY=
NEXT_PUBLIC_POSTHOG_HOST=
SENTRY_DSN=
NEXT_PUBLIC_APP_URL=
```

### Where to get them

- `DATABASE_URL` — Neon PostgreSQL connection string (Neon dashboard)
- `NEXT_PUBLIC_CLERK_PUBLISHABLE_KEY` — Clerk publishable key for frontend auth (Clerk dashboard)
- `CLERK_SECRET_KEY` — Clerk secret key for server-side auth (Clerk dashboard)
- `CLERK_WEBHOOK_SECRET` — Clerk webhook signing secret for user sync (Clerk dashboard)
- `STRIPE_SECRET_KEY` — Stripe secret key for payment processing (Stripe dashboard)
- `NEXT_PUBLIC_STRIPE_PUBLISHABLE_KEY` — Stripe publishable key for frontend (Stripe dashboard)
- `STRIPE_WEBHOOK_SECRET` — Stripe webhook signing secret (Stripe dashboard)
- `STRIPE_FAMILY_PRICE_ID` — Stripe price ID for the Family plan (Stripe dashboard)
- `STRIPE_CLASSROOM_PRICE_ID` — Stripe price ID for the Classroom plan (Stripe dashboard)
- `OPENAI_API_KEY` — OpenAI API key for book and activity generation (OpenAI platform)
- `RESEND_API_KEY` — Resend API key for transactional emails (Resend dashboard)
- `NEXT_PUBLIC_POSTHOG_KEY` — PostHog project API key for analytics (PostHog dashboard)
- `NEXT_PUBLIC_POSTHOG_HOST` — PostHog ingestion host URL (PostHog dashboard)
- `SENTRY_DSN` — Sentry DSN for error monitoring (Sentry dashboard)
- `NEXT_PUBLIC_APP_URL` — Public base URL of the deployed application (Vercel environment)

## Scripts

- `npm run dev` — start development server
- `npm run build` — production build
- `npm run start` — start production server
- `npm run db:push` — push schema to database (dev)
- `npm run db:migrate` — run migrations (production)
- `npm run db:studio` — open Prisma Studio

## Pages

- `/` — Landing
- `/onboarding` — Onboarding (auth required)
- `/dashboard` — Dashboard (auth required)
- `/dashboard/children` — Child Profiles (auth required)
- `/dashboard/children/[id]` — Child Detail (auth required)
- `/dashboard/books` — Books Library (auth required)
- `/dashboard/books/[id]` — Book Reader (auth required)
- `/dashboard/books/new` — Create Book (auth required)
- `/dashboard/activities` — Activities (auth required)
- `/dashboard/reports` — Progress Reports (auth required)
- `/settings` — Settings (auth required)
- `/billing` — Billing (auth required)

---

Built with Skylia · [skylia.dev](https://skylia.dev)
